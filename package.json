{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix omans-site && npm install --prefix server",
    "build": "npm run build --prefix omans-site && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "engines": {
    "node": "18.x"
  }
}
