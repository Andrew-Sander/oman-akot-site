{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "omans-site",
    "server"
  ],
  "scripts": {
    "start": "concurrently \"npm run start --prefix omans-site\" \"npm run start --prefix server\"",
    "build": "CI=false npm run build --prefix omans-site && npm run build --prefix server",
    "build:omans-site": "npm run build --prefix omans-site",
    "build:server": "npm run build --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
